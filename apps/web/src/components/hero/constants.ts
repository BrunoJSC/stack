/** Words shown in the vertical roller inside the headline (lowercase). */
export const ROTATING_FIELDS = [
	"código",
	"design",
	"áudio",
	"conteúdo",
] as const;

export const DEFAULT_ROTATE_MS = 2600;

/** Shared expo-out easing used across the hero animations. */
export const EASE_OUT_EXPO = [0.16, 1, 0.3, 1] as const;
