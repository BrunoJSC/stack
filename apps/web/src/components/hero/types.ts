export interface HeroFreelancersProps {
	/** Enables the pointer-driven parallax on grid, glow and content. */
	parallax?: boolean;
	/** Interval, in milliseconds, between word rotations. */
	rotateMs?: number;
	/** Toggles the decorative background grid. */
	showGrid?: boolean;
}
